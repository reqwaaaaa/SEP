server:
  port: 8081

spring:
  application:
    name: sep-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sep_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: "#020728Ceq"
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.pidu.system.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: pidu-school-enterprise-platform-secret-key-2024-very-long-key
  expiration: 86400000
  refresh-expiration: 604800000

logging:
  level:
    com.pidu: debug
    org.springframework.web: info

knife4j:
  enable: true
  setting:
    language: zh_cn

# 文件上传配置
file:
  upload-path: ./uploads
  access-url: http://localhost:8081/file/download

# 腾讯云COS配置(可选)
# 启用后文件将上传到腾讯云COS，否则使用本地存储
cos:
  enabled: false
  secret-id: AKIDqY5PXTDnAKI3a5OXWKvMZYLuISPAi3A4
  secret-key: jU1NWR8cM0To03wgHoERTJwLj4ED0e7M
  region: ap-shanghai
  bucket: school-enterprise-1393444897
  domain: Naiweilanlan.SEP
